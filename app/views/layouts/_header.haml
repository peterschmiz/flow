%header
	%nav.navbar.navbar-fixed-top.navbar-inverse{ :role => 'navigation'}
		.container-fluid
			.navbar-header
				%a.navbar-brand{ :href => '/'}
					Flow
			- if logged_in?
				= link_to 'Log out', logout_path, method: 'delete', class: 'btn btn-default navbar-btn navbar-right'